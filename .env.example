# Copy to .env and fill in what you use (all optional — configurable
# from the app's Settings panel too, but env vars take precedence).
OPENAI_API_KEY=
CLAUDE_API_KEY=
# Use host.docker.internal to reach Ollama running on the host machine.
OLLAMA_BASE_URL=http://host.docker.internal:11434
AI_PROVIDER=
